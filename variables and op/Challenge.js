const prompt = require("prompt-sync")();
let name = prompt("entre your name: ")
let age = prompt("entre your age please: ")
let sexe = prompt("are you(M/F): ")
let mail = prompt("entre your ")
console.log(
     "name is"+name,
     "age is"+age,
     "sexe is"+sexe,
     "mail is"+mail,
);
